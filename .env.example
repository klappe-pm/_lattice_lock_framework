# Lattice Lock Framework Configuration
# Copy this file to .env and fill in your values

# === Provider API Keys ===
# At least one provider is required for orchestration
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=

# === AWS Bedrock (Optional) ===
# Required if using Bedrock models
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# === Local Models (Optional) ===
# For using Ollama or local inference
OLLAMA_HOST=http://localhost:11434

# === Feature Flags ===
# Comma-separated list of features to disable (e.g. sheriff,gauntlet,feedback)
# Available: sheriff, gauntlet, feedback, rollback, consensus, mcp
LATTICE_DISABLED_FEATURES=

# === Model Selection ===
# Override default model selection strategy
# Options: auto, manual, or specific model ID
LATTICE_DEFAULT_MODEL=auto

# === Model Config Override ===
# Path to external model configuration file
# LATTICE_MODELS_CONFIG_PATH=/path/to/models.yaml

# === Scorer Config Override ===
# Path to external scorer configuration file
# LATTICE_SCORER_CONFIG_PATH=/path/to/scorer_config.yaml

# === Logging ===
# Levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
LATTICE_LOG_LEVEL=INFO

# === Database ===
# Connection string for governance database
DATABASE_URL=sqlite:///lattice.db

# === Admin API ===
# Secrets for Admin API authentication
ADMIN_SECRET_KEY=
JWT_SECRET_KEY=

# === Testing ===
# Force env var overrides for testing purposes
# LATTICE_FORCE_ENV_OVERRIDE=false
