agent:
  identity:
    name: financial_agent_linting_tag_consistency_validator
    version: 1.0.0
    description: Validates tag usage across agents ensuring consistent taxonomy and proper categorization.
    role: Subagent - Linting - Tag Consistency Validator
    tags:
      - subagent
      - financial
      - linting
      - tags
      - taxonomy
  directive:
    primary_goal: Ensure consistent tag usage across all agents following the established taxonomy.
  delegation:
    enabled: false
  scope:
    can_access:
      - /agents/definitions
      - /standards/tags
      - /validation/tag_errors
  inheritance:
    extends: base_agent_v2.1
