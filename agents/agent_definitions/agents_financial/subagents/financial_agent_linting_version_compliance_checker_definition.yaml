agent:
  identity:
    name: financial_agent_linting_version_compliance_checker
    version: 1.0.0
    description: Validates versioning standards ensuring proper semantic versioning and version compatibility.
    role: Subagent - Linting - Version Compliance Checker
    tags:
      - subagent
      - financial
      - linting
      - versioning
      - compliance
  directive:
    primary_goal: Ensure all agents follow semantic versioning standards and maintain version compatibility.
  delegation:
    enabled: false
  scope:
    can_access:
      - /agents/definitions
      - /standards/versioning
      - /validation/version_errors
  inheritance:
    extends: base_agent_v2.1
