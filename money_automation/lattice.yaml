# Lattice Lock Configuration
# Generated for: money_automation
# Documentation: https://github.com/klappe-pm/lattice-lock-framework

version: v2.1
generated_module: types_money_automation

# Entity definitions (contracts, services, etc.)
entities:
  # Example entity - customize as needed
  ExampleService:
    description: "Example service entity"
    fields:
      id:
        type: uuid
        primary_key: true
      name:
        type: str
      status:
        type: enum
        enum: [active, inactive, pending]
        default: pending

# Governance rules
config:
  # Imports that should never be used
  forbidden_imports:
    - os.system
    - subprocess.call
    - eval
    - exec

  # Required patterns for code quality
  required_patterns:
    - type_hints
    - docstrings

  # Validation settings
  validation:
    strict_mode: false
    fail_on_warning: false
