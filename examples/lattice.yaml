name: "Example Project"
version: "v1.0.0"
governance:
  level: "strict"
  policies:
    - "no-direct-db-access"
generated_module: "src.generated"
entities:
  User:
    fields:
      id:
        type: "str"
      name:
        type: "str"
